import { ParallaxOneDirective } from './parallax-one.directive';

describe('ParallaxOneDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxOneDirective();
    expect(directive).toBeTruthy();
  });
});
